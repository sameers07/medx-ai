services:
  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=medx
      - POSTGRES_PASSWORD=medx
      - POSTGRES_DB=medx
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U medx"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://medx:medx@db:5432/medx
    volumes:
      - .:/app
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: .
    command: streamlit run frontend/app.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://api:8000
    volumes:
      - .:/app
    depends_on:
      - api

volumes:
  postgres_data:
