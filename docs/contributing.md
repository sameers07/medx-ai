# Contributing

1. Branch from `develop`: `git checkout -b feature/<name>`
2. Keep commits small and scoped to one concern.
3. Open a PR into `develop`. Merge into `main` only from a stable `develop`.
4. Run `pytest` before pushing.
