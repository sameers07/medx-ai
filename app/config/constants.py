"""App-wide constants that aren't environment- or ML-config (see settings.py / config.yaml)."""

APP_TITLE = "MedX AI Platform"
APP_VERSION = "0.2.0"

HEALTH_STATUS_OK = "healthy"

REQUEST_ID_HEADER = "X-Request-ID"
PROCESS_TIME_HEADER = "X-Process-Time"
